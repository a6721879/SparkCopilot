name: SparkCopilot
options:
  bundleIdPrefix: com.songshicheng
targets:
  SparkCopilot:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - path: ios-client
        excludes:
          - Info.plist
    settings:
      INFOPLIST_FILE: ios-client/Info.plist
      GENERATE_INFOPLIST_FILE: NO



